<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Product List</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary: #059669;
      --primary-light: #10b981;
      --primary-dark: #047857;
      --secondary: #34d399;
      --accent: #6ee7b7;
      --dark: #1f2937;
      --gray-50: #f9fafb;
      --gray-100: #f3f4f6;
      --gray-200: #e5e7eb;
      --gray-300: #d1d5db;
      --gray-400: #9ca3af;
      --gray-500: #6b7280;
      --gray-600: #4b5563;
      --gray-700: #374151;
      --gray-800: #1f2937;
      --gray-900: #111827;
      --white: #ffffff;
      --text: #1f2937;
      --text-light: #6b7280;
      --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
      --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      --radius: 6px;
      --radius-lg: 8px;
      --radius-xl: 12px;
    }

    * { 
      margin: 0; 
      padding: 0; 
      box-sizing: border-box; 
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--gray-50);
      color: var(--text);
      line-height: 1.5;
    }

    .container {
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 20px;
    }

    /* Header Styles */
    header {
      background-color: var(--white);
      backdrop-filter: blur(20px);
      box-shadow: var(--shadow-sm);
      position: sticky;
      top: 0;
      z-index: 100;
      border-bottom: 1px solid var(--gray-200);
    }

    .header-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 14px 0;
    }

    .logo {
      font-size: 22px;
      font-weight: 700;
      color: var(--primary);
      text-decoration: none;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: all 0.3s ease;
    }

    .logo:hover {
      transform: translateY(-1px);
    }

    .logo-icon {
      width: 32px;
      height: 32px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      border-radius: var(--radius);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 16px;
    }

    nav ul {
      display: flex;
      list-style: none;
      gap: 20px;
    }

    nav ul li a {
      text-decoration: none;
      color: var(--text);
      font-weight: 500;
      font-size: 14px;
      padding: 6px 10px;
      border-radius: var(--radius);
      transition: all 0.3s ease;
      position: relative;
    }

    nav ul li a:hover {
      color: var(--primary);
      background-color: var(--gray-100);
    }

    .cart-icon {
      position: relative;
      font-size: 16px;
      padding: 8px !important;
      background-color: var(--gray-100);
      border-radius: var(--radius);
    }

    .cart-count {
      position: absolute;
      top: 0;
      right: 0;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      color: white;
      font-size: 9px;
      font-weight: 600;
      width: 16px;
      height: 16px;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      border: 2px solid white;
    }

    /* Hero Section */
    .hero {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
      color: white;
      padding: 50px 0;
      margin-bottom: 40px;
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="hero-pattern" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23hero-pattern)"/></svg>');
      opacity: 0.5;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      text-align: center;
      max-width: 600px;
      margin: 0 auto;
    }

    .hero h1 { 
      font-size: 2.4rem; 
      font-weight: 700;
      margin-bottom: 16px;
      line-height: 1.2;
    }

    .hero p { 
      font-size: 1rem; 
      opacity: 0.9;
      font-weight: 400;
      line-height: 1.5;
    }

    /* Products Section */
    .products-section { 
      padding: 40px 0 60px;
    }

    .section-header {
      text-align: center;
      margin-bottom: 40px;
    }

    .section-title {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 10px;
      color: var(--text);
      position: relative;
    }

    .section-subtitle {
      font-size: 0.9rem;
      color: var(--text-light);
      max-width: 450px;
      margin: 0 auto;
    }

    .section-title::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 50%;
      transform: translateX(-50%);
      width: 50px;
      height: 3px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      border-radius: 2px;
    }

    /* Product Grid - 5 CARDS PER ROW */
    ul.card-container {
      list-style-type: none;
      display: grid;
      grid-template-columns: repeat(5, 1fr); /* Exactly 5 columns */
      gap: 16px; /* Small gap between cards */
    }

    li.card {
      background-color: var(--white);
      border-radius: var(--radius-lg);
      overflow: hidden;
      box-shadow: var(--shadow);
      border: 1px solid var(--gray-200);
      display: flex;
      flex-direction: column;
      transition: all 0.3s ease;
      position: relative;
    }

    li.card:hover {
      transform: translateY(-3px);
      box-shadow: var(--shadow-lg);
      border-color: var(--primary);
    }

    .card-img {
      height: 140px; /* Much smaller image height */
      background: linear-gradient(135deg, var(--gray-100), var(--gray-200));
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      position: relative;
    }

    .card-img::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(45deg, transparent 30%, rgba(255,255,255,0.1) 50%, transparent 70%);
      transform: translateX(-100%);
      transition: transform 0.6s ease;
    }

    li.card:hover .card-img::before {
      transform: translateX(100%);
    }

    .card-img img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      cursor: zoom-in;
      transition: transform 0.3s ease;
    }

    li.card:hover .card-img img {
      transform: scale(1.05);
    }

    .card-content { 
      padding: 12px; /* Much smaller padding */
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .card-category {
      font-size: 0.65rem; /* Very small category text */
      color: var(--primary);
      text-transform: uppercase;
      font-weight: 600;
      margin-bottom: 4px;
      letter-spacing: 0.3px;
    }

    .product-name {
      font-weight: 600;
      margin-bottom: 4px;
      font-size: 0.9rem; /* Smaller product name */
      color: var(--text);
      line-height: 1.2;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }

    .product-price {
      color: var(--primary);
      font-weight: 700;
      font-size: 1.1rem; /* Smaller price */
      margin-bottom: 6px;
    }

    .product-info {
      display: flex;
      flex-direction: column;
      gap: 3px; /* Very small gaps */
      margin-bottom: 8px;
    }

    .product-quantity,
    .product-variant {
      font-size: 0.7rem; /* Very small info text */
      color: var(--text-light);
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .product-quantity i,
    .product-variant i {
      color: var(--primary);
      width: 12px;
      font-size: 0.65rem;
    }

    .card-description {
      font-size: 0.7rem; /* Very small description */
      color: var(--text-light);
      margin-bottom: 10px;
      line-height: 1.3;
      flex: 1;
      display: -webkit-box;
      -webkit-line-clamp: 2; /* Only 2 lines */
      -webkit-box-orient: vertical;
      overflow: hidden;
    }

    /* Buttons - MUCH SMALLER */
    .btn, li.card a {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 4px; /* Small gap */
      padding: 6px 10px; /* Much smaller padding */
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      color: white;
      border: none;
      border-radius: var(--radius);
      font-family: 'Inter', sans-serif;
      font-weight: 600;
      font-size: 0.7rem; /* Very small font */
      cursor: pointer;
      transition: all 0.3s ease;
      text-align: center;
      text-decoration: none;
      position: relative;
      overflow: hidden;
    }

    .btn::before, li.card a::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, var(--primary-light), var(--secondary));
      transition: left 0.3s ease;
      z-index: -1;
    }

    .btn:hover::before, li.card a:hover::before {
      left: 0;
    }

    .btn:hover, li.card a:hover {
      transform: translateY(-1px);
      box-shadow: var(--shadow);
    }

    .button-group {
      display: flex;
      flex-direction: column;
      gap: 4px; /* Very small gap */
      margin-top: auto;
    }

    .btn-secondary {
      background: linear-gradient(135deg, var(--secondary), var(--accent)) !important;
    }

    .btn-secondary::before {
      background: linear-gradient(135deg, var(--accent), var(--primary-light)) !important;
    }

    .out-of-stock {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 4px;
      background-color: #fef2f2;
      padding: 6px 10px;
      border-radius: var(--radius);
      text-align: center;
      margin-top: auto;
      color: #dc2626;
      font-weight: 600;
      font-size: 0.7rem;
      border: 1px solid #fecaca;
    }

    /* Floating Cart Button */
    .cart-button {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      color: white;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: var(--shadow-xl);
      text-decoration: none;
      font-size: 18px;
      transition: all 0.3s ease;
      z-index: 99;
      border: 2px solid white;
    }

    .cart-button:hover { 
      transform: scale(1.1);
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    }

    /* Footer */
    footer {
      background: linear-gradient(135deg, var(--gray-900), var(--gray-800));
      color: white;
      padding: 40px 0 25px;
      margin-top: 50px;
    }

    .footer-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 25px;
      margin-bottom: 25px;
    }

    .footer-col h4 {
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 16px;
      position: relative;
      color: white;
    }

    .footer-col h4::after {
      content: '';
      position: absolute;
      left: 0;
      bottom: -5px;
      width: 30px;
      height: 2px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      border-radius: 2px;
    }

    .footer-col ul { 
      list-style: none; 
    }

    .footer-col ul li { 
      margin-bottom: 8px;
    }

    .footer-col ul li a {
      color: var(--gray-300);
      text-decoration: none;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 5px;
      font-size: 0.85rem;
    }

    .footer-col ul li a:hover {
      color: var(--primary-light);
      transform: translateX(3px);
    }

    .copyright {
      text-align: center;
      padding-top: 20px;
      border-top: 1px solid var(--gray-700);
      font-size: 0.75rem;
      color: var(--gray-400);
    }

    /* Modal */
    .modal {
      display: none;
      position: fixed;
      z-index: 999;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0,0,0,0.9);
      backdrop-filter: blur(5px);
    }

    .modal-content {
      margin: 5% auto;
      display: block;
      max-width: 90%;
      max-height: 80%;
      border-radius: var(--radius-lg);
      animation: modalZoom 0.3s ease-out;
      box-shadow: var(--shadow-xl);
    }

    .modal-close {
      position: absolute;
      top: 20px;
      right: 30px;
      color: white;
      font-size: 28px;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s ease;
      width: 36px;
      height: 36px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
    }

    .modal-close:hover {
      background: rgba(255, 255, 255, 0.2);
      transform: rotate(90deg);
    }

    @keyframes modalZoom {
      from { 
        transform: scale(0.8); 
        opacity: 0; 
      }
      to { 
        transform: scale(1); 
        opacity: 1; 
      }
    }

    /* Responsive Design */
    @media (max-width: 1200px) {
      ul.card-container {
        grid-template-columns: repeat(4, 1fr); /* 4 cards on smaller screens */
      }
    }

    @media (max-width: 900px) {
      ul.card-container {
        grid-template-columns: repeat(3, 1fr); /* 3 cards on tablets */
      }
    }

    @media (max-width: 600px) {
      ul.card-container {
        grid-template-columns: repeat(2, 1fr); /* 2 cards on mobile */
        gap: 12px;
      }

      .container {
        padding: 0 12px;
      }

      .header-container {
        padding: 10px 0;
      }

      .logo {
        font-size: 18px;
      }

      .logo-icon {
        width: 28px;
        height: 28px;
        font-size: 14px;
      }

      nav ul {
        gap: 12px;
      }

      nav ul li a {
        padding: 4px 8px;
        font-size: 12px;
      }

      .hero {
        padding: 30px 0;
        margin-bottom: 30px;
      }

      .hero h1 {
        font-size: 1.8rem;
      }

      .hero p {
        font-size: 0.9rem;
      }

      .section-title {
        font-size: 1.6rem;
      }

      .card-content {
        padding: 10px;
      }

      .cart-button {
        width: 44px;
        height: 44px;
        bottom: 16px;
        right: 16px;
        font-size: 16px;
      }

      .footer-container {
        gap: 20px;
      }
    }

    @media (max-width: 400px) {
      ul.card-container {
        grid-template-columns: 1fr; /* 1 card on very small screens */
      }

      .hero h1 {
        font-size: 1.5rem;
      }

      .card-content {
        padding: 8px;
      }
    }
  </style>
</head>

<body>
<header>
  <div class="container header-container">
    <a href="/" class="logo">
      <div class="logo-icon">
        <i class="fas fa-shopping-bag"></i>
      </div>
      <span>ShopNow</span>
    </a>
    <nav>
      <ul>
        <li><a href="/"><i class="fas fa-home"></i> Home</a></li>
        <li><a href="https://www.facebook.com/GSPOrMin/"><i class="fas fa-phone"></i> Contact</a></li>
        <!-- <li><a href="/cart" class="cart-icon"><i class="fas fa-shopping-cart"></i><span class="cart-count"></span></a></li> -->
      </ul>
    </nav>
  </div>
</header>

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>Discover Your Scouting Essentials</h1>
      <p>Explore our collection of quality Girl Scout gear and supplies — perfect for every adventure!</p>
    </div>
  </div>
</section>

<section class="products-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Available Products</h2>
      <p class="section-subtitle">Browse through our carefully curated selection of premium scouting gear</p>
    </div>

    <ul class="card-container">
      <% products.forEach(product => { %>
      <li class="card">
        <div class="card-img">
          <img src="<%= product.image || 'https://via.placeholder.com/240x140?text=' + product.name %>" alt="<%= product.name %>">
        </div>
        <div class="card-content">
          <div class="card-category"><%= product.category %></div>
          <div class="product-name"><%= product.name %></div>
          <div class="product-price">₱<%= product.price %></div>
          
          <div class="product-info">
            <div class="product-quantity">
              <i class="fas fa-box"></i>
              <span><%= product.quantity > 10 ? 'In Stock' : product.quantity + ' left' %></span>
            </div>
            <div class="product-variant">
              <i class="fas fa-ruler"></i>
              <span><%= product.variant || 'N/A' %></span>
            </div>
          </div>

          <div class="card-description"><%= product.description || 'No description provided.' %></div>

          <% if (product.quantity > 0) { %>
          <div class="button-group">
            <a href="/add-to-cart/<%= product._id %>">
              <i class="fas fa-cart-plus"></i>
              Add to Cart
            </a>
            <a href="/add-to-cart/<%= product._id %>?redirect=cart" class="btn-secondary">
              <i class="fas fa-bookmark"></i>
              Reserve Now
            </a>
          </div>
          <% } else { %>
          <div class="out-of-stock">
            <i class="fas fa-times-circle"></i>
            Out of Stock
          </div>
          <% } %>
        </div>
      </li>
      <% }); %>
    </ul>
  </div>
</section>

<footer>
  <div class="container footer-container">
    <div class="footer-col">
      <h4>About Us</h4>
      <ul>
        <li><a href="#"><i class="fas fa-book"></i> Our Story</a></li>
        <li><a href="#"><i class="fas fa-heart"></i> Mission & Values</a></li>
        <li><a href="#"><i class="fas fa-star"></i> Testimonials</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Customer Service</h4>
      <ul>
        <li><a href="https://www.facebook.com/GSPOrMin/"><i class="fas fa-headset"></i> Contact Us</a></li>
        <li><a href="#"><i class="fas fa-question-circle"></i> FAQs</a></li>
        <li><a href="#"><i class="fas fa-truck"></i> Shipping Policy</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Connect With Us</h4>
      <ul>
        <li><a href="https://www.facebook.com/GSPOrMin/"><i class="fab fa-facebook"></i> Facebook</a></li>
        <li><a href="#"><i class="fab fa-instagram"></i> Instagram</a></li>
        <li><a href="#"><i class="fab fa-twitter"></i> Twitter</a></li>
      </ul>
    </div>
  </div>
  <div class="copyright">
    © 2025 ShopNow. All Rights Reserved. | Designed with ❤️ for Girl Scouts
  </div>
</footer>

<div id="imageModal" class="modal">
  <span class="modal-close">&times;</span>
  <img class="modal-content" id="modalImage">
</div>

<a href="/cart" class="cart-button">
  <i class="fas fa-shopping-cart"></i>
</a>

<script>
  const modal = document.getElementById('imageModal');
  const modalImg = document.getElementById('modalImage');
  const closeModal = document.querySelector('.modal-close');

  document.querySelectorAll('.card-img img').forEach(img => {
    img.addEventListener('click', () => {
      modal.style.display = "block";
      modalImg.src = img.src;
    });
  });

  closeModal.onclick = () => modal.style.display = "none";
  
  window.onclick = (e) => {
    if (e.target === modal) modal.style.display = "none";
  };

  // Add smooth scroll behavior
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      document.querySelector(this.getAttribute('href')).scrollIntoView({
        behavior: 'smooth'
      });
    });
  });
</script>

</body>
</html>