<!DOCTYPE html>
<html lang="en">
<head>
    <title>Create Event</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: #f5f5f5;
            color: #222;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            margin-top: 40px;
            font-size: 2.5rem;
            color: #2e7d32;
            text-align: center;
        }

        .form-container {
            background: #ffffff;
            max-width: 600px;
            width: 90%;
            margin: 30px auto;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
        }

        #notification-count {
            text-align: center;
            margin-bottom: 20px;
            font-size: 1rem;
            color: #1b5e20;
            font-weight: 600;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        .input-group {
            position: relative;
            margin-bottom: 25px;
        }

        .input-group i {
            position: absolute;
            top: 50%;
            left: 16px;
            transform: translateY(-50%);
            color: #4caf50;
            font-size: 1rem;
        }

        .input-group input,
        .input-group textarea {
            width: 100%;
            padding: 14px 16px 14px 44px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            outline: none;
            font-size: 1rem;
            background: #fafafa;
            transition: 0.3s;
        }

        .input-group input:focus,
        .input-group textarea:focus {
            border-color: #66bb6a;
            background: #ffffff;
            box-shadow: 0 0 0 3px rgba(102, 187, 106, 0.1);
        }

        .input-group textarea {
            resize: none;
            height: 120px;
        }

        button {
            padding: 14px;
            background-color: #2e7d32;
            color: white;
            font-size: 1rem;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: 0.3s;
            font-weight: bold;
        }

        button:hover {
            background-color: #1b5e20;
            transform: translateY(-1px);
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.15);
        }

        @media (max-width: 640px) {
            .form-container {
                padding: 25px;
            }

            h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>

    <h1><i class="fas fa-calendar-plus"></i> Create an Event</h1>

    <div id="notification-count">
        <i class="fas fa-bell"></i> Notifications: <%= notificationsCount %>
    </div>

    <div class="form-container">
        <form action="/admin/event" method="POST">
            <div class="input-group">
                <i class="fas fa-heading"></i>
                <input type="text" name="title" placeholder="Event Title" required>
            </div>

            <div class="input-group">
                <i class="fas fa-align-left"></i>
                <textarea name="description" placeholder="Description" required></textarea>
            </div>

            <div class="input-group">
                <i class="fas fa-calendar-alt"></i>
                <input type="date" name="date" required>
            </div>

            <div class="input-group">
                <i class="fas fa-clock"></i>
                <input type="time" name="time" required>
            </div>

            <div class="input-group">
                <i class="fas fa-map-marker-alt"></i>
                <input type="text" name="location" placeholder="Location" required>
            </div>

            <div class="input-group">
                <i class="fas fa-envelope"></i>
                <input type="email" name="email" placeholder="Enter email for notification" required>
            </div>

            <button type="submit"><i class="fas fa-paper-plane"></i> Submit Event</button>
        </form>
    </div>

</body>
</html>
    